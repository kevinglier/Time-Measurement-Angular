import { TmTimeTableTimePipe } from './tm-time-table-time.pipe';

describe('TmTimeTableTimePipe', () => {
  it('create an instance', () => {
    const pipe = new TmTimeTableTimePipe();
    expect(pipe).toBeTruthy();
  });
});
